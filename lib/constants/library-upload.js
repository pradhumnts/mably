/** Defaults for client-side checks when plan-specific props are not passed yet. */
export {
  STARTER_LIBRARY_MAX_FILE_BYTES as LIBRARY_MAX_UPLOAD_BYTES,
  STARTER_LIBRARY_MAX_FILE_LABEL as LIBRARY_MAX_UPLOAD_LABEL,
} from "@/lib/billing/library-storage-policy";
