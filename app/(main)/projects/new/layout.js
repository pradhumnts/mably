export const metadata = {
  title: "Create New Project",
  description: "Create a new project and invite your client",
};

export default function NewProjectLayout({ children }) {
  return children;
}

