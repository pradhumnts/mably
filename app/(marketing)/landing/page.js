import { MablyLandingPage } from "@/components/marketing/mably-landing-page";

export const metadata = {
  title: "Mably — Client portals built for freelancers",
  description:
    "Give every client a branded project portal: activity, library, payments, and chat — without the inbox chaos.",
};

export default function LandingPage() {
  return <MablyLandingPage />;
}
